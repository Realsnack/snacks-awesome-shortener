meta {
  name: GET /
  type: http
  seq: 1
}

get {
  url: {{base_url}}/
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.body: eq Hello, world!
}

settings {
  encodeUrl: true
  timeout: 0
}
